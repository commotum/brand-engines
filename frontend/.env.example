# Leave empty for the local launcher: Next proxies /api to Django on port 8017.
# Optional: set a separate API origin without /api when running your own backend.
# That backend must allow the frontend's origin for cross-origin requests.
NEXT_PUBLIC_API_URL=
